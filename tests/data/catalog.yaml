plugins:
  source:
    - module: intake_xarray
sources:
  xarray_source:
    description: example xarray source plugin
    driver: netcdf
    args: 
      urlpath: '{{ CATALOG_DIR }}/example_1.nc'
      chunks: {}